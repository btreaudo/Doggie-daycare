<%= form_for(@favorite) do |f| %>
  <% if @favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% @favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dob %><br>
    <%= f.number_field :dob %>
  </div>
  <div class="field">
    <%= f.label :favorite_drink %><br>
    <%= f.text_field :favorite_drink %>
  </div>
  <div class="field">
    <%= f.label :favorite_sport %><br>
    <%= f.text_field :favorite_sport %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
